FROM scratch

ARG BOARD
ARG BASE_ROOTFS_TGZ
ARG QEMU_RUNNER_ARCH
ARG QEMU_RUNNER_STATIC_PLACE
ARG LOCALE
ARG TIMEZONE
ARG REPO_URL

ADD "$BASE_ROOTFS_TGZ" /
COPY "qemu-$QEMU_RUNNER_ARCH-static" "$QEMU_RUNNER_STATIC_PLACE"
