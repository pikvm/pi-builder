FROM scratch

ARG BOARD
ARG BASE_ROOTFS_TGZ
ARG QEMU_GUEST_ARCH
ARG QEMU_STATIC_GUEST_PATH
ARG LOCALE
ARG TIMEZONE
ARG REPO_URL

ADD "$BASE_ROOTFS_TGZ" /
COPY "qemu-$QEMU_GUEST_ARCH-static" "$QEMU_STATIC_GUEST_PATH"
